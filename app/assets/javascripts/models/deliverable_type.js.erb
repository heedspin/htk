function DeliverableType(attributes) {
	HtkModel.call(this, attributes);
}

DeliverableType.prototype = Object.create(HtkModel.prototype, {
	api_url : {	value : function(){ return "/api/v1/deliverable_types"; } },
	type_key : { value : "deliverable_type" },
	attribute_keys : { value : <%= DeliverableType.columns.map(&:name).to_json %> }
});

